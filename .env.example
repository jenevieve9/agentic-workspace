# 可选：将 GitHub Token 写入环境变量（仅适用于 PUBLIC 仓库；否则 Token 会打包进前端代码，存在泄露风险）
# 推荐做法：直接在「系统联动」页面手动粘贴 Token，更私密、更安全。
# Token 需勾选 gist 权限（GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)）
VITE_GITHUB_TOKEN=
